# project-cards

> Project Cards App

## Build Setup

``` bash
# install dependencies
$ yarn install

# serve with hot reload at localhost:3000
$ yarn start:dev

# build for production and launch server
$ yarn build
$ yarn start
```
